name: mfar_superapp
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  mfar_miniapp:
    git:
      url: https://github.com/firdaus-agmo/mfar_miniapp.git
      ref: main
  provider: ^6.1.5+1
  permission_handler: ^12.0.1
  webview_flutter: ^4.13.0
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.5.1
  build_runner: ^2.8.0

flutter:
  uses-material-design: true
